# tasks

A decent task application.

## Development

    git clone https://github.com/JaniM/tasks.git
    cd tasks
    npm install
    npm start

For builds, just run

    npm run dist -- --linux
    npm run dist -- --win portable
